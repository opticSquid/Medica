spring:
  profiles:
    active: dev
  application:
    name: APPOINTMENT
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
logging:
  level:
    web: error