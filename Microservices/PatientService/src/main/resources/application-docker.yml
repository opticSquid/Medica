spring:
  datasource:
    url: jdbc:mysql://mysqldb:3306/patients
    username: soumalya
    password: QsCAzX@#$%24680
  jpa:
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create-drop
    show-sql: true
server:
  port: 0
eureka:
  client:
    service-url:
      defaultZone: http://service-discovery:8761/eureka
logging:
  level:
    web: debug